# 家での開発・スマホ公開ガイド

NeuronQ（テストアプリ）を自宅のPCで引き続き開発したり、スマートフォンで利用できるようにするための手順をまとめました。

## 1. 持ち帰るべきファイル
このアプリはサーバー等の複雑な設定が不要なフロントエンド（HTML/CSS/JS）のみで構成されています。
以下のフォルダを丸ごとコピーして持ち帰るだけで、家のPC（Windows/Mac）でも開発可能です。

📂 **対象フォルダ:**
`Z:\開発課題\アプリ開発\0313\testアプリ`

📄 **必要なファイル一覧（合計5ファイル）:**
- `index.html` （アプリの画面構造・UI）
- `style.css` （デザイン・レイアウト）
- `data.js` （アプリ内で出題される全問題データ）
- `app_core.js` （データ保存、テーマ切り替えなどの基本機能）
- `app_features.js` （テスト実行、分析画面などの機能）

※ *教科書の写真データは既にテキスト化されて `data.js` に含まれているため、写真ファイル自体はアプリの動作には不要です。*

---

## 2. 家での開発（起動）方法
1. 持ち帰ったフォルダを家のPCに保存します。
2. フォルダ内にある **`index.html` をダブルクリック** するか、ブラウザ（Google Chromeなど）にドラッグ＆ドロップします。
3. これだけで、学校のPCと全く同じようにアプリが起動し、開発の続きが行えます。
4. プログラムを変更した後は、ブラウザを「リロード（再読み込み）」すれば最新の状態が反映されます。

---

## 3. スマホでも利用できるようにする（GitHub Pages）
スマホでも学習したい場合、GitHubの無料機能を使ってWeb上に公開するのが一番簡単で安全です。

### 公開手順
1. ブラウザで [GitHub](https://github.com/) にアクセスし、ログインします。
2. 画面左上などの **「New」** ボタンから、新しいリポジトリを作成します。
   - Repository name: `neuronq-app` など任意の名前を入力
   - Public（公開）が選択されていることを確認して **「Create repository」** をクリックします。
3. 次の画面で **「uploading an existing file」** という青いリンクをクリックします。
4. さきほどの5つのファイル（`index.html` 等）をすべて画面内に**ドラッグ＆ドロップ**し、「Commit changes」ボタンを押してアップロードします。
5. 画面上部の **「Settings」** タブを開き、左メニューから **「Pages」** を選択します。
6. 「Build and deployment」セクションの「Branch」を **「None」から「main」** に変更し、「Save」を押します。
7. 1〜2分ほど待つと、画面上部に以下のようなURLが表示されます。
   `Your site is live at https://[ユーザー名].github.io/neuronq-app/`

このURLをスマホに送って開けば、普通のWebアプリとして利用できます！
スマホのブラウザメニューから**「ホーム画面に追加」**をしておくと、次から全画面で使いやすくなります。

---

> [!TIP]
> **今後のデータ更新について**
> 家で `data.js` に新しい問題を追加した場合は、同じ手順で GitHub に `data.js` を再度ドラッグ＆ドロップして上書き（Commit）するだけで、スマホ側にも新しい問題が反映されます。
